package com.aditya.trucker.validation;

/**
 * Validation group for PATCH (partial update) operations.
 * Fields annotated with this group will only be validated during PATCH requests.
 */
public interface UpdateGroup {
    // Marker interface for validation groups
}
